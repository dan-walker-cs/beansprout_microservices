package dev.danwalkercs.sproutpass.controller;

import org.springframework.web.bind.annotation.RequestMapping;

/**
 * Base Controller interface for all endpoints not requiring authentication.
 */
@RequestMapping("/open")
public interface OpenController {
}
